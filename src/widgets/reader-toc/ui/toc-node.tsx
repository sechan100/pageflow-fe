'use client'
import { SxProps } from "@mui/material";
import { TocFolder } from "../model/toc-context";

type TocFolderProps = {
  folder: TocFolder;
  sx?: SxProps;
}
const TocFolderNode = ({
  sx
}: TocFolderProps) => {

  return (
    <>

    </>
  )
}

type TocSectionProps = {
  sx?: SxProps;
}
export const TocSectionNode = ({
  sx
}: TocSectionProps) => {

  return (
    <>

    </>
  )
}